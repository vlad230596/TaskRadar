node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db
coverage/
.vite/
.claude/
CREDENTIALS.local.md

# Подпись Android-релиза. Дублирует app/android/.gitignore намеренно: keystore
# нельзя закоммитить даже случайно и даже не из той папки — потеря ключа
# означает, что обновить установленное приложение больше нельзя никогда.
# Инструкция — app/RELEASE-ANDROID.md.
key.properties
*.jks
*.keystore
