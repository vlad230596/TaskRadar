name: CI

# Orchestrator. `CI / Required checks` is the single job worth protecting the
# branch with: adding a component workflow below extends the gate automatically
# instead of needing another branch-protection entry.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:
    inputs:
      ref:
        description: Git ref to verify
        required: false
        type: string
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to verify
        required: false
        type: string

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  backend:
    uses: ./.github/workflows/backend-ci.yml
    with:
      ref: ${{ inputs.ref }}

  integration:
    uses: ./.github/workflows/integration-ci.yml
    with:
      ref: ${{ inputs.ref }}

  # The Flutter client (F10). It joined the gate when the web bundle became
  # something this repository deploys: see the header of app-ci.yml.
  app:
    uses: ./.github/workflows/app-ci.yml
    with:
      ref: ${{ inputs.ref }}

  required:
    name: Required checks
    # `always()` plus explicit result checks, rather than relying on job
    # failure: a skipped or cancelled component must not count as a pass.
    if: always()
    needs: [backend, integration, app]
    runs-on: ubuntu-latest
    steps:
      - name: Verify every component check
        env:
          BACKEND_RESULT: ${{ needs.backend.result }}
          INTEGRATION_RESULT: ${{ needs.integration.result }}
          APP_RESULT: ${{ needs.app.result }}
        run: |
          results=(
            "$BACKEND_RESULT"
            "$INTEGRATION_RESULT"
            "$APP_RESULT"
          )
          printf 'Component results: %s\n' "${results[*]}"
          for result in "${results[@]}"; do
            test "$result" = success
          done
